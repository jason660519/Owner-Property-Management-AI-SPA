I will implement the file upload and processing feature with the following steps:

## 1. Backend Implementation (`backend/ocr_service`)
I will modify the FastAPI service to support batch file uploads and Server-Sent Events (SSE) for real-time progress.

- **Modify `src/api/routes/ocr.py`**:
    - Add an in-memory event queue manager `sse_queues`.
    - Create `POST /api/v1/ocr/upload-batch`:
        - Accepts `files: List[UploadFile]`.
        - Validates file types and sizes (double-check backend side).
        - Saves files to a secure temporary directory with randomized names.
        - Returns a `batch_id`.
        - Triggers a background task for processing.
    - Create `GET /api/v1/ocr/events/{batch_id}`:
        - Returns a `StreamingResponse` (SSE) to push progress updates.
    - Implement `process_batch_upload_background`:
        - Iterates through files, calls `ocr_processor`.
        - Emits events: `queued`, `processing`, `completed`, `failed`.
- **File Cleanup**:
    - Add a scheduled task (similar to the log archiver) to clean up temporary files older than 24 hours.

## 2. Frontend Implementation (`apps/superadmin`)
I will create a new file upload component and integrate it into the System Prompt Editor.

- **Dependencies**: Install `react-dropzone` (for drag-and-drop) and `pdfjs-dist` (for PDF preview).
- **Create `components/ai-settings/SystemPromptFileUpload.tsx`**:
    - **UI**: 
        - Drag & drop zone with validation instructions.
        - File list with 150x150px thumbnails (images) and first-page previews (PDF).
        - Progress bars and status icons.
        - "Retry" button for failed uploads.
    - **Logic**:
        - **Validation**: Check MIME types, extensions, and 10MB limit.
        - **Preview**: Use `URL.createObjectURL` for images and `pdfjs-dist` to render the first page of PDFs.
        - **Upload**: Send `FormData` to the new batch upload endpoint.
        - **Real-time Updates**: Connect to the SSE endpoint using `EventSource` to update progress.
- **Update `components/ai-settings/SystemPromptEditor.tsx`**:
    - Embed the `SystemPromptFileUpload` component, likely toggled by the "Test" button or as a permanent section below the editor.

## 3. Testing & Verification
- **Unit Tests**: Create `__tests__/SystemPromptFileUpload.test.tsx` to verify validation logic and component rendering (aiming for ≥ 80% coverage).
- **Manual Test Script**: Create `manual_test_upload.md` outlining steps to verify all 9 requirements (formats, size limits, batching, preview, accessibility, security, error handling, SSE, cross-browser).

## 4. Dependencies
- `npm install react-dropzone pdfjs-dist`
- `npm install -D @types/pdfjs-dist` (if needed)

This plan ensures separation of concerns, meets all security and functional requirements, and provides a robust user experience.