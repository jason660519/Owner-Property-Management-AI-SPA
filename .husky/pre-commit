#!/usr/bin/env sh

echo "🔍 執行 pre-commit 檢查..."

# 執行 lint-staged (允許空)
npx lint-staged --allow-empty 2>/dev/null || true

# 執行 TypeScript 型別檢查 (如果存在)
if [ -f "tsconfig.json" ]; then
  echo "🔷 檢查 TypeScript 型別..."
  npx tsc --noEmit 2>/dev/null || true
fi

echo "✅ Pre-commit 檢查通過！"
