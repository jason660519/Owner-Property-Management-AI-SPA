# Row 130 Test Index

本資料夾對應 `project-progress` 的 `Row ID 130`（Superadmin × Paperclip 開發流程整合）。

目前此功能測試採「就近放置」策略，主要腳本位置如下：

- `apps/superadmin/lib/paperclip/__tests__/`
- `apps/superadmin/app/api/paperclip/**/__tests__/`

建議執行命令：

`npm run test --workspace superadmin -- paperclip --runInBand`
