# Row 008 Unit / Integration Tests

Scope: LLM Observability Console.

Planned coverage:

- `llm-monitor/actions.ts` normalized trace/evaluation rows.
- `LLMMonitorClient.tsx` Trace Console and Evaluation Runs tabs.
- Legacy fallback from `ai_usage_logs` and `adapter_evaluation_runs`.
