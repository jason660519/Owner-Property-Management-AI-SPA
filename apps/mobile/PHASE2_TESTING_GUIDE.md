# Phase 2 測試指南

> **創建日期**: 2026-01-31
> **創建者**: Claude Sonnet 4.5

---

## 🚀 快速啟動

### 1. 安裝依賴

```bash
cd apps/mobile
npm install
```

### 2. 啟動 Supabase（如果未啟動）

```bash
cd ../..
supabase start
```

### 3. 啟動 Expo

```bash
cd apps/mobile
npm start
```

### 4. 在設備上測試

**重要**：相機功能必須在實體手機上測試，模擬器不支援相機。

- **iOS**: 掃描 QR Code 或使用 Expo Go App
- **Android**: 掃描 QR Code 或使用 Expo Go App

---

## 🧪 Phase 2 功能測試清單

### ✅ 1. 相機拍攝功能

#### 測試步驟

1. 進入「文件管理」頁面
2. 點擊「拍攝照片」按鈕
3. 授予相機權限
4. 拍攝一張文件照片
5. 確認照片被裁剪為 4:3 縱橫比
6. 確認照片自動上傳

#### 預期結果

- ✅ 相機啟動正常
- ✅ 可切換前/後鏡頭（部分設備）
- ✅ 拍攝後顯示預覽
- ✅ 照片自動壓縮（< 2MB）
- ✅ 上傳成功後列表自動刷新
- ✅ 新文件顯示在列表頂部

#### 驗證要點

```
檢查上傳的照片：
1. 打開 Supabase Dashboard
2. 進入 Storage > property-documents
3. 確認檔案大小 < 2MB
4. 確認檔案名稱格式：photo_[timestamp].jpg
```

---

### ✅ 2. 文件預覽功能

#### 測試步驟

1. 上傳或拍攝至少 3 個文件（包含圖片與 PDF）
2. 觀察文件列表中的縮圖顯示
3. 點擊任一文件卡片

#### 預期結果

**列表顯示**:
- ✅ 圖片文件顯示 72x72px 縮圖
- ✅ PDF 文件顯示紫色 PDF 圖標
- ✅ 其他文件顯示通用文件圖標
- ✅ 顯示檔案大小（KB/MB）
- ✅ 顯示副檔名（大寫）
- ✅ OCR 狀態標籤顯示正確顏色

**全屏查看**:
- ✅ 圖片全屏顯示
- ✅ 可雙指縮放
- ✅ 可拖動查看
- ✅ 右上角有關閉按鈕
- ✅ PDF 顯示「即將推出」佔位提示

#### OCR 狀態顏色驗證

| 狀態 | 顏色 | 標籤 |
|------|------|------|
| pending | 🟠 橙色 | 等待處理 |
| processing | 🔵 藍色 | 處理中 |
| completed | 🟢 綠色 | 已完成 |
| failed | 🔴 紅色 | 失敗 |
| manual_review | 🟡 黃色 | 待審核 |

---

### ✅ 3. 刪除確認對話框

#### 測試步驟

1. 點擊任一文件卡片右側的「⋮」選單按鈕
2. 觀察對話框內容
3. 測試「取消」按鈕
4. 再次點擊「⋮」選單按鈕
5. 測試「刪除」按鈕

#### 預期結果

**對話框顯示**:
- ✅ 顯示警告圖標（紅色三角形）
- ✅ 標題：「確認刪除文件？」
- ✅ 顯示文件名稱
- ✅ 顯示文件類型（中文標籤）
- ✅ 警告訊息：「此操作無法復原...」
- ✅ 兩個按鈕：「取消」與「刪除」

**操作行為**:
- ✅ 點擊「取消」關閉對話框
- ✅ 點擊「刪除」後顯示載入動畫
- ✅ 刪除成功後顯示成功提示
- ✅ 列表自動刷新，已刪除文件消失
- ✅ Supabase Storage 中的文件被刪除
- ✅ 資料庫記錄被刪除

#### 驗證要點

```sql
-- 在 Supabase SQL Editor 執行
SELECT * FROM property_documents ORDER BY created_at DESC LIMIT 5;

-- 確認刪除的文件不再存在
```

---

### ✅ 4. 雙按鈕佈局測試

#### 測試步驟

1. 觀察文件上傳區域
2. 確認有兩個按鈕並排顯示
3. 分別點擊兩個按鈕

#### 預期結果

**UI 顯示**:
- ✅ 兩個按鈕水平排列
- ✅ 左按鈕：「選擇文件」（紫色背景）
- ✅ 右按鈕：「拍攝照片」（紫色邊框，透明背景）
- ✅ 按鈕高度一致
- ✅ 按鈕間距適當（12px）

**交互行為**:
- ✅ 「選擇文件」打開文件選擇器
- ✅ 「拍攝照片」打開相機
- ✅ 上傳中兩個按鈕都被禁用
- ✅ 禁用時按鈕透明度降低

---

### ✅ 5. 空狀態顯示

#### 測試步驟

1. 刪除所有已上傳文件
2. 觀察空狀態顯示

#### 預期結果

- ✅ 顯示文件夾圖標
- ✅ 主提示：「尚未上傳任何文件」
- ✅ 副提示：「點擊上方按鈕開始上傳」
- ✅ 居中對齊
- ✅ 顏色柔和（灰色系）

---

## 🔍 進階測試

### 邊界條件測試

#### 1. 檔案大小限制

- ❌ 嘗試上傳 > 10MB 的檔案
- ✅ 應顯示錯誤：「檔案大小超過 10MB 限制」

#### 2. 相機權限拒絕

- ❌ 拒絕相機權限
- ✅ 應顯示錯誤：「需要相機權限」

#### 3. 網絡離線

- ❌ 關閉網絡後嘗試上傳
- ✅ 應顯示網絡錯誤

#### 4. 並發上傳

- 🔄 快速點擊「選擇文件」兩次
- ✅ 按鈕應被禁用，避免重複上傳

---

## 📊 性能測試

### 列表滾動流暢度

#### 測試步驟

1. 上傳 20+ 個文件
2. 快速滾動列表

#### 預期結果

- ✅ 60 FPS 流暢滾動
- ✅ 縮圖載入流暢
- ✅ 無明顯卡頓

### 記憶體使用

#### 測試步驟

1. 上傳多個大圖片（5-10MB）
2. 反覆進入/退出全屏查看
3. 觀察記憶體使用

#### 預期結果

- ✅ 記憶體穩定增長
- ✅ 退出全屏後記憶體釋放
- ✅ 無記憶體洩漏

---

## 🎨 視覺回歸測試

### 設計一致性檢查

#### 顏色

- ✅ 背景色統一：`#1A1A1A`
- ✅ 卡片背景統一：`#262626`
- ✅ 品牌色統一：`#7C3AED`
- ✅ 文字顏色統一：主 `#FFF`, 次 `#999`, 弱 `#666`

#### 間距

- ✅ 頁面 padding: `16px`
- ✅ 卡片間距: `12px`
- ✅ 內容間距: `8px`

#### 圓角

- ✅ 小元素: `8px`
- ✅ 卡片: `12px`
- ✅ 對話框: `16px`

#### 字體大小

- ✅ 標題: `18px` / `20px`
- ✅ 內文: `14px` / `16px`
- ✅ 小字: `11px` / `12px`

---

## 🐛 已知問題與限制

### 1. PDF 預覽功能

- ⚠️ **狀態**: 未實作
- 📝 **顯示**: 「PDF 預覽功能即將推出」佔位提示
- 🔮 **計畫**: Phase 3 或 4 加入

### 2. 詳細上傳進度

- ⚠️ **狀態**: 簡化版本（0% → 50% → 100%）
- 📝 **限制**: 無法顯示實時上傳速度
- 🔮 **計畫**: Phase 3 優化

### 3. 批次上傳

- ⚠️ **狀態**: 單檔案上傳
- 📝 **限制**: 無法一次選擇多個文件
- 🔮 **計畫**: Phase 4 加入

---

## 🔧 故障排除

### 相機無法啟動

**原因**: 權限未授予
**解決**:
```
iOS: 設定 > 隱私權 > 相機 > Expo Go
Android: 設定 > 應用程式 > Expo > 權限 > 相機
```

### 縮圖無法顯示

**原因**: Supabase Storage 權限問題
**解決**:
```sql
-- 確認 storage.objects 有 SELECT 權限
-- 檢查 property-documents bucket 是否為 public
```

### 刪除失敗

**原因**: RLS 政策限制
**解決**:
```sql
-- 確認有 DELETE 權限
-- 檢查 owner_id 是否匹配當前用戶
```

---

## 📝 測試報告範本

### 測試記錄

```
測試日期: 2026-01-31
測試者: [Your Name]
測試設備: iPhone 15 Pro / Android Pixel 8
Expo 版本: 54.x
React Native 版本: 0.76.x

✅ 通過測試 (5/5):
- 相機拍攝功能
- 文件預覽功能
- 刪除確認對話框
- 雙按鈕佈局
- 空狀態顯示

❌ 失敗測試 (0/5):
- 無

⚠️ 待優化 (3/3):
- PDF 預覽功能（未實作）
- 詳細上傳進度（簡化版）
- 批次上傳（未實作）

備註:
- 所有核心功能運作正常
- UI 設計一致性良好
- 無明顯性能問題
```

---

## 🎓 測試技巧

### 1. 快速重置測試環境

```bash
# 清空 Supabase Storage
supabase storage empty property-documents

# 清空資料庫記錄
# 在 Supabase SQL Editor 執行：
DELETE FROM property_documents WHERE owner_id = 'your-user-id';
```

### 2. 使用 React Native Debugger

```bash
# 安裝
brew install --cask react-native-debugger

# 啟動
open "rndebugger://set-debugger-loc?host=localhost&port=8081"
```

### 3. 查看 Expo Logs

```bash
# 啟動時添加 --dev-client 參數
npm start -- --dev-client

# 或使用 tunnel 模式
npm start -- --tunnel
```

---

## ✨ 下一步

Phase 2 測試完成後，準備進入 **Phase 3: OCR 整合**：

1. Realtime OCR 狀態訂閱
2. OCR 結果預覽與編輯
3. OCR 失敗重試機制

查看完整計畫：[Phase 3 Implementation Plan](../../docs/progress-reports/PHASE3_IMPLEMENTATION_PLAN.md)

---

**測試愉快！** 🚀
