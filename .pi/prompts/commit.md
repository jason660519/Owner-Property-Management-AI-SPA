Review the current `git diff --staged` and `git status`.
Write a commit message following the project convention: `<type>: <description>`

Types: feat / fix / docs / refactor / style / test / chore

- Description in Traditional Chinese
- If multiple changes span different areas, use the most prominent type
- Keep the first line under 72 characters
- Add a body paragraph if changes are non-trivial

Stage relevant files and commit. Do NOT push.
