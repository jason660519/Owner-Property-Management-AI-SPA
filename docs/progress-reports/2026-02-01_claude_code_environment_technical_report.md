> **創建日期**: 2026-02-01
> **創建者**: Claude Opus 4.5
> **最後修改**: 2026-02-01
> **修改者**: Claude Opus 4.5
> **版本**: 1.2

---

# Claude Code 開發環境技術報告

## 📋 目錄

1. [概述](#概述)
2. [CLAUDE.md 詳細檢測報告](#claudemd-詳細檢測報告)
3. [Rules 詳細檢測報告](#rules-詳細檢測報告)
4. [Skills 詳細檢測報告](#skills-詳細檢測報告)
5. [Hooks 詳細檢測報告](#hooks-詳細檢測報告)
6. [設定檔清單與用途說明](#設定檔清單與用途說明)
7. [MCP 伺服器連線狀態檢測](#mcp-伺服器連線狀態檢測)
8. [控制範圍與權限邊界](#控制範圍與權限邊界)
9. [設定載入與執行優先順序](#設定載入與執行優先順序)
10. [設定檔相依性與衝突分析](#設定檔相依性與衝突分析)
11. [錯誤排查步驟與解決方案](#錯誤排查步驟與解決方案)
12. [功能驗證測試方法](#功能驗證測試方法)
13. [架構圖與流程圖](#架構圖與流程圖)

---

## 概述

本專案「Owner Property Management AI SPA」採用 Claude Code 作為主要 AI 開發輔助工具，整合了多層次的設定架構來確保 AI 協作的一致性與品質。

### 技術棧總覽

| 類型 | 技術 | 版本 |
|:-----|:-----|:-----|
| AI 工具 | Claude Code | Latest |
| 前端 (Web) | Next.js | 15 |
| 前端 (Mobile) | Expo | 54 |
| 後端 | Supabase | Local |
| 資料庫 | PostgreSQL | 17 |
| 協議 | MCP (Model Context Protocol) | 1.0 |

---

## CLAUDE.md 詳細檢測報告

### 檔案清單與層級

| 檔案位置 | 層級 | 狀態 | 檔案大小 | 版本 |
|:---------|:-----|:-----|:---------|:-----|
| `~/.claude/CLAUDE.md` | 全域 (Global) | ✅ 存在 | 5,077 bytes | - |
| `./CLAUDE.md` | 專案 (Project) | ✅ 存在 | 15,165 bytes | v2.6 |

### 使用時機與觸發條件

| 檔案 | 使用時機 | 觸發條件 | 自動載入 |
|:-----|:---------|:---------|:---------|
| `~/.claude/CLAUDE.md` | Claude Code 啟動時 | 任何專案開啟 | ✅ 是 |
| `./CLAUDE.md` | 進入專案目錄時 | 工作目錄包含此檔案 | ✅ 是 |

### 影響控制範圍 (Scope)

| 設定項目 | 全域 CLAUDE.md | 專案 CLAUDE.md | 覆蓋規則 |
|:---------|:---------------|:---------------|:---------|
| 回覆語言 | 繁體中文 | 繁體中文 | 專案優先 |
| 程式碼註解語言 | 英文 | 英文 | 一致 |
| 檔案命名規則 | ❌ 未定義 | ✅ PascalCase/camelCase/kebab-case | 專案定義 |
| AI 身份標記 | ❌ 未定義 | ✅ [Claude] 前綴格式 | 專案定義 |
| Git Commit 格式 | Conventional Commits | `[AI名稱] type(scope): message` | 專案擴展 |
| 歸檔位置規則 | ❌ 未定義 | ✅ docs/, apps/, backend/ | 專案定義 |
| TypeScript 嚴格模式 | ✅ strict mode | ✅ strict mode | 一致 |
| 禁止 any 類型 | ✅ 是 | ✅ 是 | 一致 |

### 全域 CLAUDE.md 規範內容

| 規範類別 | 內容摘要 | 強制性 |
|:---------|:---------|:-------|
| 回覆風格 | 繁體中文、簡潔直接 | 🟡 建議 |
| TypeScript | strict mode、避免 any | 🔴 強制 |
| Python | PEP 8、type hints | 🔴 強制 |
| Git Commit | `type: description` 格式 | 🟡 建議 |
| 錯誤處理 | Fail Fast、明確訊息 | 🟡 建議 |
| 測試覆蓋率 | 80%+ (核心 90%+) | 🟡 建議 |

### 專案 CLAUDE.md 規範內容

| 規範類別 | 內容摘要 | 強制性 | 影響範圍 |
|:---------|:---------|:-------|:---------|
| 檔案命名 | PascalCase/camelCase/kebab-case | 🔴 強制 | 所有程式碼檔案 |
| 歸檔規則 | docs/, apps/, backend/, supabase/ | 🔴 強制 | 所有新建檔案 |
| AI 身份標記 | 文件 Metadata、程式碼 Header | 🔴 強制 | 所有 AI 產出 |
| Git Commit | `[AI名稱] type(scope): msg` | 🔴 強制 | 所有 commit |
| 禁止行為 | 根目錄創建文檔、中文檔名 | 🔴 強制 | 全專案 |
| Context7 參考 | 技術文檔查詢路徑表 | 🟡 建議 | 文檔查詢時 |
| Monorepo 結構 | apps/web, apps/mobile, backend | 🔴 強制 | 專案架構 |

### 檢測結果

| 檢測項目 | 結果 | 備註 |
|:---------|:-----|:-----|
| 檔案存在性 | ✅ 通過 | 兩層都存在 |
| Metadata 格式 | ✅ 通過 | 專案版本 v2.6 |
| 規範完整性 | ✅ 通過 | 涵蓋所有必要項目 |
| 層級覆蓋邏輯 | ✅ 正確 | 專案設定覆蓋全域 |
| 修改歷史 | ✅ 完整 | 記錄從 v1.0 到 v2.6 |

---

## Rules 詳細檢測報告

### Rules 檔案清單

| 檔案路徑 | 檔案大小 | 最後修改 | 狀態 |
|:---------|:---------|:---------|:-----|
| `.claude/rules/general.md` | 3,097 bytes | 2026-01-30 | ✅ 存在 |
| `.claude/rules/frontend/react-expo.md` | 4,105 bytes | 2026-01-30 | ✅ 存在 |
| `.claude/rules/backend/supabase.md` | 4,021 bytes | 2026-01-30 | ✅ 存在 |

### 使用時機與觸發條件

| Rule 檔案 | 觸發時機 | Path 匹配模式 | 自動載入 |
|:----------|:---------|:--------------|:---------|
| `general.md` | **永遠** | 無限制 (全專案) | ✅ 是 |
| `react-expo.md` | 編輯前端檔案時 | `apps/web/**/*.{ts,tsx}`, `apps/mobile/**/*.{ts,tsx}`, `packages/**/*.{ts,tsx}` | ✅ 條件式 |
| `supabase.md` | 編輯資料庫相關時 | `supabase/**/*.sql`, `supabase/**/*.toml`, `apps/web/**/*.ts`, `apps/mobile/**/*.ts`, `**/lib/supabase.ts` | ✅ 條件式 |

### 影響控制範圍 (Scope)

| Rule | 影響範圍 | 控制內容 | 優先級 |
|:-----|:---------|:---------|:-------|
| `general.md` | 全專案所有檔案 | 命名規範、Git 工作流、程式碼風格、語言偏好 | 基礎層 |
| `react-expo.md` | 前端 TypeScript 檔案 | React 19/Next.js 15/Expo 54 規範、Server/Client Component、樣式系統 | 前端層 |
| `supabase.md` | 資料庫與 SDK 相關 | 表命名規範、Migration 格式、RLS 政策、SDK 初始化範例 | 後端層 |

### 各 Rule 詳細規範內容

#### general.md (全域規則)

| 規範項目 | 內容 | 強制性 |
|:---------|:-----|:-------|
| React 組件命名 | PascalCase.tsx | 🔴 強制 |
| Hooks 命名 | camelCase + use 前綴 | 🔴 強制 |
| 工具函數命名 | camelCase.ts | 🔴 強制 |
| 資料夾命名 | kebab-case | 🔴 強制 |
| 縮排 | 2 空格 | 🔴 強制 |
| 字串引號 | 單引號優先 | 🟡 建議 |
| Commit Type | feat/fix/docs/refactor/style/test/chore | 🔴 強制 |
| 分支命名 | feature/*, fix/*, docs/* | 🟡 建議 |

#### react-expo.md (前端規則)

| 規範項目 | 內容 | 適用範圍 |
|:---------|:-----|:---------|
| 預設 Server Components | 無需標註 | Next.js |
| Client Components | 需要 'use client' | 互動組件 |
| 樣式系統 (Web) | Tailwind CSS | apps/web |
| 樣式系統 (Mobile) | StyleSheet / NativeWind | apps/mobile |
| 狀態管理優先順序 | useState → URL Params → Context → Zustand | 全前端 |
| 禁止 HTML 標籤 | 使用 View/Text 替代 | Expo |

#### supabase.md (資料庫規則)

| 規範項目 | 內容 | 範例 |
|:---------|:-----|:-----|
| 表名格式 | snake_case | `property_listings` |
| 欄位格式 | snake_case | `created_at` |
| 主鍵命名 | `id` (UUID) | - |
| 外鍵命名 | `xxx_id` | `user_id` |
| Migration 命名 | `YYYYMMDDHHMMSS_desc.sql` | `20260115120000_add_status.sql` |
| RLS 政策 | 必須啟用 | `auth.uid() = user_id` |

### Rules 載入矩陣

| 編輯檔案類型 | general.md | react-expo.md | supabase.md |
|:-------------|:-----------|:--------------|:------------|
| `apps/web/app/page.tsx` | ✅ | ✅ | ✅ |
| `apps/mobile/src/components/Button.tsx` | ✅ | ✅ | ✅ |
| `supabase/migrations/xxx.sql` | ✅ | ❌ | ✅ |
| `apps/web/lib/supabase/client.ts` | ✅ | ✅ | ✅ |
| `docs/README.md` | ✅ | ❌ | ❌ |

### 檢測結果

| 檢測項目 | 結果 | 備註 |
|:---------|:-----|:-----|
| 檔案存在性 | ✅ 通過 | 3 個 rules 都存在 |
| Frontmatter 格式 | ✅ 通過 | paths 設定正確 |
| Path 匹配測試 | ✅ 通過 | glob 模式有效 |
| 內容完整性 | ✅ 通過 | 涵蓋必要規範 |
| 規則衝突 | ✅ 無衝突 | 各層級互補 |

---

## Skills 詳細檢測報告

### Skills 檔案清單

#### 專案級 Skills

| Skill 名稱 | 檔案路徑 | 狀態 | 檔案大小 |
|:-----------|:---------|:-----|:---------|
| `python-security-scan` | `.claude/skills/python-security-scan/SKILL.md` | ✅ 存在 | 7,697 bytes |

#### 全域 Skills (~/.claude/skills/)

| Skill 名稱 | 類型 | 狀態 | 用途 |
|:-----------|:-----|:-----|:-----|
| `keybindings-help` | 目錄 | ✅ 存在 | 鍵盤快捷鍵設定輔助 |
| `clickhouse-io` | 目錄 | ✅ 存在 | ClickHouse 資料庫模式 |
| `coding-standards` | 目錄 | ✅ 存在 | 通用編碼標準 |
| `backend-patterns` | 目錄 | ✅ 存在 | 後端架構模式 |
| `design-md` | 符號連結 | ✅ 存在 | 設計系統文件產生 |
| `continuous-learning` | 目錄 | ✅ 存在 | 自動學習模式萃取 |
| `tdd-workflow` | 目錄 | ✅ 存在 | TDD 開發流程 |
| `frontend-patterns` | 目錄 | ✅ 存在 | 前端開發模式 |
| `security-review` | 目錄 | ✅ 存在 | 安全審查 |
| `strategic-compact` | 目錄 | ✅ 存在 | 策略性上下文壓縮 |

### 使用時機與觸發條件

| Skill 名稱 | 觸發方式 | 觸發條件 | 自動執行 |
|:-----------|:---------|:---------|:---------|
| `python-security-scan` | 用戶呼叫 `/python-security-scan` | 明確指令 | ❌ 否 |
| `keybindings-help` | 用戶詢問鍵盤設定 | 關鍵字匹配 | ✅ 自動建議 |
| `coding-standards` | 程式碼審查時 | AI 判斷需要 | ✅ 自動參考 |
| `backend-patterns` | 後端開發時 | AI 判斷需要 | ✅ 自動參考 |
| `frontend-patterns` | 前端開發時 | AI 判斷需要 | ✅ 自動參考 |
| `tdd-workflow` | 撰寫測試時 | AI 判斷需要 | ✅ 自動參考 |
| `security-review` | 安全相關開發時 | AI 判斷需要 | ✅ 自動參考 |
| `strategic-compact` | 上下文過長時 | AI 判斷需要 | ✅ 自動建議 |
| `continuous-learning` | 完成任務後 | AI 判斷需要 | ✅ 自動執行 |
| `design-md` | 設計系統分析 | 用戶請求 | ❌ 否 |

### 影響控制範圍 (Scope)

| Skill | 影響範圍 | 控制內容 | 產出物 |
|:------|:---------|:---------|:-------|
| `python-security-scan` | Python 專案 | OWASP Top 10 掃描、CVE 檢查 | 安全報告 |
| `keybindings-help` | Claude Code 設定 | ~/.claude/keybindings.json | 設定檔 |
| `coding-standards` | 所有程式碼 | 編碼風格建議 | 程式碼審查 |
| `backend-patterns` | 後端程式碼 | API 設計、資料庫優化 | 架構建議 |
| `frontend-patterns` | 前端程式碼 | React 模式、效能優化 | 最佳實踐 |
| `tdd-workflow` | 測試相關 | 80%+ 覆蓋率要求 | 測試程式碼 |
| `security-review` | 敏感功能 | 認證、輸入驗證、密鑰 | 安全清單 |
| `strategic-compact` | 對話上下文 | 上下文壓縮策略 | 壓縮建議 |
| `continuous-learning` | 完成的任務 | 模式萃取 | 學習檔案 |
| `design-md` | 設計檔案 | Stitch 專案分析 | DESIGN.md |

### python-security-scan Skill 詳細資訊

| 項目 | 內容 |
|:-----|:-----|
| 掃描類型 | Quick Scan / Full Scan / Targeted Scan |
| 支援框架 | Flask, Django, FastAPI |
| 檢測項目 | OWASP Top 10、注入攻擊、反序列化、硬編碼密鑰 |
| 嚴重等級 | CRITICAL / HIGH / MEDIUM / LOW / INFO |
| 輸出格式 | Markdown 報告、SARIF、JSON、JUnit XML |
| 附帶腳本 | `dependency-audit.sh`, `pattern-scanner.py`, `secret-scanner.py` |
| 參考文件 | 7 個參考文件 (owasp-top-10.md, flask-security.md 等) |

### 檢測結果

| 檢測項目 | 結果 | 備註 |
|:---------|:-----|:-----|
| 專案 Skill 存在性 | ✅ 通過 | python-security-scan 完整 |
| 全域 Skills 存在性 | ✅ 通過 | 10 個 skills 可用 |
| SKILL.md 格式 | ✅ 通過 | name/description frontmatter 正確 |
| 附帶資源完整性 | ✅ 通過 | scripts/, references/, assets/ 存在 |
| 符號連結有效性 | ✅ 通過 | design-md 連結有效 |

---

## Hooks 詳細檢測報告

### Hooks 檔案清單

| Hook 名稱 | 檔案路徑 | 狀態 | 執行權限 | 檔案大小 |
|:----------|:---------|:-----|:---------|:---------|
| `pre-commit` | `.husky/pre-commit` | ✅ 存在 | ✅ 可執行 | 347 bytes |
| `commit-msg` | `.husky/commit-msg` | ✅ 存在 | ✅ 可執行 | 1,160 bytes |
| `pre-push` | `.husky/pre-push` | ✅ 存在 | ✅ 可執行 | 450 bytes |

### 使用時機與觸發條件

| Hook | 觸發時機 | 觸發條件 | 可中斷 |
|:-----|:---------|:---------|:-------|
| `pre-commit` | `git commit` 執行前 | 有 staged 變更 | ✅ 可中斷 (exit 1) |
| `commit-msg` | commit 訊息輸入後 | 任何 commit | ✅ 可中斷 (exit 1) |
| `pre-push` | `git push` 執行前 | 任何 push | ✅ 可中斷 (exit 1) |

### 影響控制範圍 (Scope)

| Hook | 影響範圍 | 控制內容 | 失敗後果 |
|:-----|:---------|:---------|:---------|
| `pre-commit` | 待 commit 的檔案 | lint-staged、TypeScript 型別檢查 | ⚠️ 目前設定為不阻擋 (`\|\| true`) |
| `commit-msg` | commit 訊息 | Conventional Commits 格式驗證 | 🔴 阻擋 commit |
| `pre-push` | 待 push 的變更 | npm test、npm build | ⚠️ 目前設定為不阻擋 |

### 各 Hook 詳細邏輯

#### pre-commit Hook

| 步驟 | 執行內容 | 失敗處理 | 狀態 |
|:-----|:---------|:---------|:-----|
| 1 | 執行 lint-staged | `\|\| true` (不阻擋) | ⚠️ 寬鬆 |
| 2 | 檢查 tsconfig.json 存在 | 條件執行 | ✅ 正常 |
| 3 | 執行 `npx tsc --noEmit` | `\|\| true` (不阻擋) | ⚠️ 寬鬆 |

#### commit-msg Hook

| 步驟 | 執行內容 | 驗證規則 | 狀態 |
|:-----|:---------|:---------|:-----|
| 1 | 讀取 commit 訊息 | - | ✅ 正常 |
| 2 | 正則驗證格式 | `^(feat\|fix\|docs\|...)(\(.+\))?: .{1,100}$` | ✅ 嚴格 |
| 3 | 失敗時顯示說明 | 列出所有 type 類型 | ✅ 正常 |
| 4 | `exit 1` 阻擋 | 🔴 強制 | ✅ 正常 |

**支援的 Commit Types:**

| Type | 說明 | 範例 |
|:-----|:-----|:-----|
| `feat` | 新功能 | `feat(auth): 新增 Google 登入` |
| `fix` | 錯誤修復 | `fix(property): 修復搜尋錯誤` |
| `docs` | 文件變更 | `docs: 更新 API 文件` |
| `style` | 程式碼格式 | `style: 統一縮排` |
| `refactor` | 程式碼重構 | `refactor: 優化查詢` |
| `test` | 測試相關 | `test: 新增單元測試` |
| `chore` | 雜項 | `chore: 更新依賴` |
| `perf` | 效能優化 | `perf: 優化載入速度` |
| `ci` | CI/CD 設定 | `ci: 更新 workflow` |
| `build` | 建置系統 | `build: 升級 webpack` |
| `revert` | 還原提交 | `revert: 還原上次變更` |

#### pre-push Hook

| 步驟 | 執行內容 | 失敗處理 | 狀態 |
|:-----|:---------|:---------|:-----|
| 1 | 執行 `npm run test` | 無 script 則跳過 | ⚠️ 寬鬆 |
| 2 | 檢查 build script 存在 | 條件執行 | ✅ 正常 |
| 3 | 執行 `npm run build` | `\|\| echo` (不阻擋) | ⚠️ 寬鬆 |

### Hooks 與 Claude Code 的互動

| 情境 | Claude Code 行為 | Hook 影響 |
|:-----|:-----------------|:----------|
| Claude 執行 `git commit` | 需遵守 commit-msg 格式 | 🔴 必須遵守 |
| Claude 建立 commit 訊息 | 需使用 `[Claude] type(scope): msg` | 🔴 必須遵守 |
| Claude 執行 `git push` | 觸發 pre-push 檢查 | ⚠️ 可能失敗但不阻擋 |
| Claude 修改檔案後 commit | 觸發 pre-commit 檢查 | ⚠️ 可能警告但不阻擋 |

### 檢測結果

| 檢測項目 | 結果 | 備註 |
|:---------|:-----|:-----|
| 檔案存在性 | ✅ 通過 | 3 個 hooks 都存在 |
| 執行權限 | ✅ 通過 | 都有 executable 權限 |
| Husky 初始化 | ⚠️ 需確認 | 未見 `_/husky.sh` 引用於 pre-commit |
| commit-msg 格式驗證 | ✅ 嚴格 | 會阻擋不符格式的 commit |
| pre-commit 阻擋力 | ⚠️ 寬鬆 | 使用 `\|\| true` 不阻擋 |
| pre-push 阻擋力 | ⚠️ 寬鬆 | 使用 `\|\| echo` 不阻擋 |

### 建議改進

| 項目 | 現況 | 建議 |
|:-----|:-----|:-----|
| pre-commit lint-staged | 失敗不阻擋 | 移除 `\|\| true` 以強制執行 |
| pre-commit tsc | 失敗不阻擋 | 移除 `\|\| true` 以強制類型檢查 |
| pre-push test | 失敗不阻擋 | 考慮移除 `2>/dev/null` 顯示錯誤 |
| Husky 初始化 | pre-commit 未引用 husky.sh | 加入 `. "$(dirname -- "$0")/_/husky.sh"` |

---

### 1. 全域設定（用戶層級）

| 檔案路徑 | 用途 | 類型 |
|:---------|:-----|:-----|
| `~/.claude/CLAUDE.md` | 用戶個人全域開發偏好 | Markdown |
| `~/.claude/settings.json` | 全域權限設定 | JSON |
| `~/.claude/skills/` | 全域可用技能集合 | 目錄 |

#### `~/.claude/CLAUDE.md` 內容摘要

```markdown
# 個人全域開發偏好
- 語言：繁體中文
- 程式碼註解：使用英文
- TypeScript strict mode
- 遵循 PEP 8 規範 (Python)
```

#### `~/.claude/settings.json`

```json
{
  "permissions": {
    "allow": []
  }
}
```

---

### 2. 專案層級設定

| 檔案路徑 | 用途 | 強制性 |
|:---------|:-----|:-------|
| `CLAUDE.md` | 專案主要 AI 行為規範 | 🔴 強制 |
| `.claude/settings.json` | 專案插件設定 | 選用 |
| `.claude/settings.local.json` | 本地權限與 MCP 設定 | 🔴 強制 |
| `.claude/.mcp.json` | 專案級 MCP 伺服器設定 | 選用 |
| `.mcp.json` | 根目錄 MCP 設定 | 選用 |

#### `CLAUDE.md` 核心規範（v2.6）

```markdown
## 核心規範
- 檔案命名與歸檔規則
- AI 身份標記規範
- Git Commit 格式要求
- TypeScript 嚴格模式
- 文件歸檔位置規則
```

#### `.claude/settings.json`

```json
{
  "enabledPlugins": {
    "document-skills@anthropic-agent-skills": true,
    "everything-claude-code@everything-claude-code": true
  }
}
```

#### `.claude/settings.local.json` 權限設定

```json
{
  "permissions": {
    "allow": [
      "WebFetch(domain:docs.google.com)",
      "Bash(git add:*)",
      "Bash(git commit -m \"...\")",
      "Bash(git push)",
      "Bash(supabase status:*)",
      "Bash(supabase migration:*)",
      "Bash(npm install:*)",
      "Bash(npx tsc:*)",
      "Bash(ls:*)",
      "Bash(chmod:*)",
      "Bash(npm run lint)"
    ]
  },
  "enabledMcpjsonServers": [
    "context7", "github", "postgres", "slack", "notion",
    "openai", "anthropic", "googlemaps", "memory", "docker",
    "firecrawl", "gdrive"
  ],
  "enableAllProjectMcpServers": true
}
```

---

### 3. Rules 設定

| 檔案路徑 | 適用範圍 | 觸發條件 |
|:---------|:---------|:---------|
| `.claude/rules/general.md` | 整個專案 | 永遠載入 |
| `.claude/rules/frontend/react-expo.md` | 前端檔案 | `apps/web/**/*.{ts,tsx}`, `apps/mobile/**/*.{ts,tsx}` |
| `.claude/rules/backend/supabase.md` | 資料庫相關 | `supabase/**/*.sql`, `**/lib/supabase.ts` |

#### Rules 載入機制

```yaml
---
paths:
  - 'apps/web/**/*.{ts,tsx}'
  - 'apps/mobile/**/*.{ts,tsx}'
---
# 當編輯符合上述路徑的檔案時，此 rule 自動載入
```

---

### 4. Skills 設定

#### 專案級 Skills

| Skill 名稱 | 檔案位置 | 用途 |
|:-----------|:---------|:-----|
| `python-security-scan` | `.claude/skills/python-security-scan/` | Python 安全掃描 |

#### 全域 Skills（~/.claude/skills/）

| Skill 名稱 | 用途 |
|:-----------|:-----|
| `keybindings-help` | 鍵盤快捷鍵設定 |
| `clickhouse-io` | ClickHouse 資料庫模式 |
| `coding-standards` | 編碼標準 |
| `backend-patterns` | 後端架構模式 |
| `design-md` | 設計系統文件產生 |
| `continuous-learning` | 自動學習模式萃取 |
| `tdd-workflow` | TDD 開發流程 |
| `frontend-patterns` | 前端開發模式 |
| `security-review` | 安全審查 |
| `strategic-compact` | 策略性上下文壓縮 |

---

### 5. Hooks 設定（Git Hooks via Husky）

| Hook 名稱 | 檔案位置 | 功能 |
|:----------|:---------|:-----|
| `pre-commit` | `.husky/pre-commit` | lint-staged + TypeScript 檢查 |
| `commit-msg` | `.husky/commit-msg` | Conventional Commits 格式驗證 |
| `pre-push` | `.husky/pre-push` | 測試執行 + 建置檢查 |

#### pre-commit Hook

```bash
#!/usr/bin/env sh
echo "🔍 執行 pre-commit 檢查..."
npx lint-staged --allow-empty 2>/dev/null || true
if [ -f "tsconfig.json" ]; then
  echo "🔷 檢查 TypeScript 型別..."
  npx tsc --noEmit 2>/dev/null || true
fi
echo "✅ Pre-commit 檢查通過！"
```

#### commit-msg Hook（Conventional Commits）

```bash
PATTERN="^(feat|fix|docs|style|refactor|test|chore|perf|ci|build|revert)(\(.+\))?: .{1,100}$"
```

---

## MCP 伺服器連線狀態檢測

### 專案 MCP 設定（.mcp.json）

| 伺服器名稱 | 套件 | 狀態 |
|:-----------|:-----|:-----|
| context7 | @upstash/context7-mcp | ⚠️ 需驗證 API Key |
| github | @anthropic/mcp-github | ⚠️ 需驗證 Token |
| postgres | @anthropic/mcp-postgres | ✅ 可用（需 DATABASE_URL） |
| slack | @anthropic/mcp-slack | ⚠️ 未設定 Token |
| notion | @anthropic/mcp-notion | ⚠️ 未設定 |
| openai | @anthropic/mcp-openai | ⚠️ 需驗證 API Key |
| anthropic | @anthropic/mcp-anthropic | ✅ 可用 |
| googlemaps | @anthropic/mcp-googlemaps | ⚠️ 未設定 |
| memory | @anthropic/mcp-memory | ✅ 可用 |
| docker | @anthropic/mcp-docker | ✅ 可用 |
| firecrawl | @anthropic/mcp-firecrawl | ⚠️ 需驗證 API Key |
| gdrive | @anthropic/mcp-gdrive | ⚠️ 需 OAuth 設定 |

### 專案內部 MCP 設定（.claude/.mcp.json）

| 伺服器名稱 | 套件 | 環境變數 | 狀態 |
|:-----------|:-----|:---------|:-----|
| context7 | @upatra/mcp-context7 | CONTEXT7_API_KEY | ✅ 已設定 |
| brave-search | @modelcontextprotocol/server-brave-search | BRAVE_API_KEY | ❌ 未設定 |
| postgres | @modelcontextprotocol/server-postgres | DATABASE_URL | ✅ 已設定 |
| playwright | @executeautomation/playwright-mcp-server | - | ✅ 可用 |
| filesystem | @modelcontextprotocol/server-filesystem | - | ✅ 可用 |
| github | @modelcontextprotocol/server-github | GITHUB_PERSONAL_ACCESS_TOKEN | ✅ 已設定 |
| slack | @modelcontextprotocol/server-slack | SLACK_BOT_TOKEN, SLACK_TEAM_ID | ❌ 未設定 |
| memory | @modelcontextprotocol/server-memory | - | ✅ 可用 |
| time | @modelcontextprotocol/server-everything | - | ✅ 可用 |
| fetch | @modelcontextprotocol/server-fetch | - | ✅ 可用 |

### 環境變數狀態檢查

| 變數名稱 | 設定狀態 | 來源 |
|:---------|:---------|:-----|
| ANTHROPIC_API_KEY | ✅ 已設定 | .env |
| CONTEXT7_API_KEY | ✅ 已設定 | .env |
| GITHUB_PERSONAL_ACCESS_TOKEN | ✅ 已設定 | .env |
| DATABASE_URL | ✅ 已設定 | .env |
| SLACK_BOT_TOKEN | ❌ 空白 | .env |
| SLACK_TEAM_ID | ❌ 空白 | .env |
| BRAVE_API_KEY | ❌ 未設定 | - |

---

## 控制範圍與權限邊界

### 設定層級架構

```
┌─────────────────────────────────────────────────────────────┐
│                    Global Level (最低優先)                    │
│  ~/.claude/CLAUDE.md, ~/.claude/settings.json               │
│  適用範圍: 所有專案                                           │
├─────────────────────────────────────────────────────────────┤
│                    Project Level (中優先)                     │
│  CLAUDE.md, .claude/settings.json                           │
│  適用範圍: 本專案所有檔案                                      │
├─────────────────────────────────────────────────────────────┤
│                    Local Level (最高優先)                     │
│  .claude/settings.local.json                                │
│  適用範圍: 本機開發環境                                        │
├─────────────────────────────────────────────────────────────┤
│                    Rules Level (條件式)                       │
│  .claude/rules/**/*.md                                      │
│  適用範圍: 符合 paths 條件的檔案                               │
└─────────────────────────────────────────────────────────────┘
```

### 權限邊界

| 權限類型 | 允許操作 | 限制 |
|:---------|:---------|:-----|
| 檔案讀取 | 所有專案檔案 | 無限制 |
| 檔案寫入 | 專案目錄內 | 需遵守命名規則 |
| Bash 執行 | 白名單命令 | 見 settings.local.json |
| Git 操作 | add, commit, push | 需遵守 commit 格式 |
| 網路存取 | WebFetch 白名單 | 僅 docs.google.com |
| MCP 工具 | 已啟用伺服器 | 需有效 API Key |

---

## 設定載入與執行優先順序

### 載入順序圖

```
啟動 Claude Code
       │
       ▼
┌──────────────────┐
│ 1. 載入全域設定    │
│ ~/.claude/       │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ 2. 載入專案 CLAUDE.md │
│ (根目錄)           │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ 3. 載入 .claude/ 設定 │
│ settings.json     │
│ settings.local.json │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ 4. 初始化 MCP 伺服器 │
│ .mcp.json         │
│ .claude/.mcp.json │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ 5. 載入 Skills    │
│ .claude/skills/  │
│ ~/.claude/skills/ │
└────────┬─────────┘
         │
         ▼
┌──────────────────────────┐
│ 6. 條件式載入 Rules       │
│ (依編輯檔案路徑決定)       │
└──────────────────────────┘
```

### 優先順序規則

1. **Local > Project > Global**：較接近專案的設定覆蓋較遠的
2. **Explicit > Implicit**：明確設定覆蓋預設行為
3. **Rules 為增量式**：多個 rules 可同時生效，不互相覆蓋

---

## 設定檔相依性與衝突分析

### 相依性矩陣

| 設定檔 | 依賴 | 被依賴 |
|:-------|:-----|:-------|
| CLAUDE.md | 無 | rules, skills |
| settings.local.json | .env (環境變數) | MCP 伺服器 |
| .mcp.json | 環境變數 | 工具呼叫 |
| rules/*.md | CLAUDE.md 規範 | 程式碼編輯 |
| skills/ | rules 規範 | 技能觸發 |
| .husky/* | package.json | git commit |

### 潛在衝突

| 衝突類型 | 描述 | 解決方案 |
|:---------|:-----|:---------|
| MCP 設定重複 | `.mcp.json` 與 `.claude/.mcp.json` 都定義相同伺服器 | 優先使用 `.claude/.mcp.json` |
| 權限衝突 | 全域禁止 vs 專案允許 | 專案設定優先 |
| Rule 路徑重疊 | 多個 rules 符合同一檔案 | 所有符合的 rules 都會載入 |

### 已識別衝突

```
⚠️ 衝突 1: MCP 伺服器定義重複
   - .mcp.json: context7 使用 @upstash/context7-mcp
   - .claude/.mcp.json: context7 使用 @upatra/mcp-context7
   → 建議: 統一使用 .claude/.mcp.json 的設定

⚠️ 衝突 2: 環境變數命名
   - .env 中未定義 BRAVE_API_KEY
   - .claude/.mcp.json 中 brave-search 需要此變數
   → 建議: 新增 BRAVE_API_KEY 或停用 brave-search
```

---

## 錯誤排查步驟與解決方案

### 常見問題 1: MCP 伺服器連線失敗

**症狀**: 工具呼叫時顯示 "MCP server not connected"

**排查步驟**:
```bash
# 1. 檢查環境變數
cat .env | grep -E "API_KEY|TOKEN"

# 2. 測試 npx 是否可用
npx --version

# 3. 手動啟動 MCP 伺服器測試
npx -y @modelcontextprotocol/server-memory

# 4. 檢查 Node.js 版本
node --version  # 需要 >= 18.0.0
```

**解決方案**:
```bash
# 確保 .env 檔案正確設定
echo "CONTEXT7_API_KEY=your-key-here" >> .env

# 重新載入 Claude Code
# 或執行 /reload 命令
```

---

### 常見問題 2: Rules 未正確載入

**症狀**: 編輯特定檔案時規則未生效

**排查步驟**:
```bash
# 1. 確認 rules 檔案存在
ls -la .claude/rules/

# 2. 檢查 frontmatter paths 設定
head -10 .claude/rules/frontend/react-expo.md

# 3. 確認檔案路徑符合 glob 模式
# paths: 'apps/web/**/*.{ts,tsx}'
```

**解決方案**:
```yaml
---
paths:
  - 'apps/web/**/*.{ts,tsx}'  # 確保路徑正確
  - 'apps/mobile/**/*.{ts,tsx}'
---
```

---

### 常見問題 3: Git Hooks 不執行

**症狀**: commit 時未顯示驗證訊息

**排查步驟**:
```bash
# 1. 確認 husky 已安裝
ls -la .husky/

# 2. 檢查執行權限
ls -la .husky/pre-commit

# 3. 確認 husky 已初始化
cat .husky/_/husky.sh
```

**解決方案**:
```bash
# 重新安裝 husky
npm install husky --save-dev
npx husky install

# 確保執行權限
chmod +x .husky/*
```

---

### 常見問題 4: Skill 無法觸發

**症狀**: 呼叫 /python-security-scan 無回應

**排查步驟**:
```bash
# 1. 確認 SKILL.md 存在且格式正確
cat .claude/skills/python-security-scan/SKILL.md | head -20

# 2. 檢查 frontmatter
# 必須包含 name 和 description
```

**解決方案**:
```yaml
---
name: 'Python Security Scan'
description: 'Comprehensive security vulnerability scanner...'
---
```

---

## 功能驗證測試方法

### 測試 1: CLAUDE.md 載入驗證

```bash
# 在 Claude Code 中執行
# 詢問: "請告訴我專案的 AI 身份標記規範是什麼？"
# 預期回答應包含: Claude Opus 4.5, [Claude] 等內容
```

### 測試 2: Rules 條件載入驗證

```bash
# 1. 開啟前端檔案
code apps/web/app/page.tsx

# 2. 在 Claude Code 中詢問技術棧
# 預期: 應提及 React 19, Next.js 15, Expo 54

```

### 測試 3: MCP 伺服器驗證

```bash
# 使用 Task 工具測試 context7
# 呼叫: 使用 Context7 查詢 Next.js App Router 文檔

# 預期: 應返回最新的 Next.js 15 文檔內容
```

### 測試 4: Git Hook 驗證

```bash
# 測試 commit-msg hook
git commit -m "invalid message"
# 預期: 顯示 "❌ 提交訊息格式錯誤！"

git commit -m "feat(test): valid message"
# 預期: 顯示 "✅ 提交訊息格式正確！"
```

### 測試 5: Skill 驗證

```bash
# 在 Claude Code 中執行
/python-security-scan

# 預期: 應觸發 Python 安全掃描流程
```

---

## 架構圖與流程圖

### Claude Code 設定架構總覽

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         Claude Code Runtime                              │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  ┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐     │
│  │  Global Config  │───▶│  Project Config │───▶│  Local Config   │     │
│  │  ~/.claude/     │    │  CLAUDE.md      │    │  .claude/       │     │
│  │                 │    │  .mcp.json      │    │  settings.local │     │
│  └────────┬────────┘    └────────┬────────┘    └────────┬────────┘     │
│           │                      │                       │               │
│           ▼                      ▼                       ▼               │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                      Merged Configuration                        │   │
│  │  • Permissions (allow/deny)                                      │   │
│  │  • MCP Servers (enabled list)                                    │   │
│  │  • Plugins (enabled/disabled)                                    │   │
│  │  • AI Behavior Rules                                             │   │
│  └──────────────────────────────┬──────────────────────────────────┘   │
│                                  │                                       │
│           ┌──────────────────────┼──────────────────────┐               │
│           ▼                      ▼                      ▼               │
│  ┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐     │
│  │     Rules       │    │     Skills      │    │  MCP Servers    │     │
│  │  (Conditional)  │    │  (User-invoke)  │    │  (Tool access)  │     │
│  ├─────────────────┤    ├─────────────────┤    ├─────────────────┤     │
│  │ • general.md    │    │ • python-scan   │    │ • context7      │     │
│  │ • react-expo.md │    │ • security-rev  │    │ • github        │     │
│  │ • supabase.md   │    │ • tdd-workflow  │    │ • postgres      │     │
│  │                 │    │ • ...           │    │ • memory        │     │
│  └─────────────────┘    └─────────────────┘    └─────────────────┘     │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│                           Git Hooks (Husky)                              │
│  ┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐     │
│  │   pre-commit    │    │   commit-msg    │    │    pre-push     │     │
│  │  • lint-staged  │    │  • Conventional │    │  • npm test     │     │
│  │  • tsc --noEmit │    │    Commits      │    │  • npm build    │     │
│  └─────────────────┘    └─────────────────┘    └─────────────────┘     │
└─────────────────────────────────────────────────────────────────────────┘
```

### 檔案編輯時的 Rules 載入流程

```
User 開啟檔案: apps/web/app/page.tsx
                    │
                    ▼
           ┌───────────────────┐
           │ 檢查檔案路徑      │
           │ apps/web/app/*.tsx │
           └─────────┬─────────┘
                     │
        ┌────────────┴────────────┐
        ▼                         ▼
┌───────────────────┐    ┌───────────────────┐
│ general.md        │    │ react-expo.md     │
│ paths: 無限制     │    │ paths: apps/web/** │
│ ✅ 載入           │    │ ✅ 載入            │
└───────────────────┘    └───────────────────┘
        │                         │
        └────────────┬────────────┘
                     ▼
           ┌───────────────────┐
           │ 合併規則生效      │
           │ • 命名規範        │
           │ • Git 工作流      │
           │ • 技術棧要求      │
           │ • 組件開發規範    │
           └───────────────────┘
```

### MCP 伺服器連線流程

```
┌─────────────────────────────────────────────────────────────────┐
│                        MCP Server Initialization                 │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
                   ┌─────────────────────┐
                   │ 讀取 .mcp.json 設定  │
                   └──────────┬──────────┘
                              │
         ┌────────────────────┼────────────────────┐
         ▼                    ▼                    ▼
   ┌───────────┐        ┌───────────┐        ┌───────────┐
   │ context7  │        │ postgres  │        │ github    │
   │           │        │           │        │           │
   │ API Key   │        │ DB URL    │        │ Token     │
   │ Required  │        │ Required  │        │ Required  │
   └─────┬─────┘        └─────┬─────┘        └─────┬─────┘
         │                    │                    │
         ▼                    ▼                    ▼
   ┌───────────┐        ┌───────────┐        ┌───────────┐
   │ 讀取 .env │        │ 讀取 .env │        │ 讀取 .env │
   │ CONTEXT7_ │        │ DATABASE_ │        │ GITHUB_   │
   │ API_KEY   │        │ URL       │        │ TOKEN     │
   └─────┬─────┘        └─────┬─────┘        └─────┬─────┘
         │                    │                    │
         ▼                    ▼                    ▼
   ┌───────────┐        ┌───────────┐        ┌───────────┐
   │ npx -y    │        │ npx -y    │        │ npx -y    │
   │ @upatra/  │        │ @mcp/     │        │ @mcp/     │
   │ context7  │        │ postgres  │        │ github    │
   └─────┬─────┘        └─────┬─────┘        └─────┬─────┘
         │                    │                    │
         ▼                    ▼                    ▼
   ┌───────────┐        ┌───────────┐        ┌───────────┐
   │ ✅/❌     │        │ ✅/❌     │        │ ✅/❌     │
   │ Connected │        │ Connected │        │ Connected │
   └───────────┘        └───────────┘        └───────────┘
```

---

## 附錄：快速參考表

### 設定檔路徑總覽

| 類型 | 檔案路徑 | 優先級 |
|:-----|:---------|:-------|
| 全域 CLAUDE.md | ~/.claude/CLAUDE.md | 1 (最低) |
| 全域設定 | ~/.claude/settings.json | 1 |
| 專案 CLAUDE.md | ./CLAUDE.md | 2 |
| 專案設定 | ./.claude/settings.json | 2 |
| 本地設定 | ./.claude/settings.local.json | 3 (最高) |
| 根目錄 MCP | ./.mcp.json | 2 |
| 專案級 MCP | ./.claude/.mcp.json | 3 |
| Rules | ./.claude/rules/**/*.md | 條件式 |
| Skills | ./.claude/skills/**/SKILL.md | 用戶觸發 |
| Git Hooks | ./.husky/* | Git 事件觸發 |

### 常用命令

```bash
# 檢查 Supabase 狀態
supabase status

# 啟動開發環境
./start-dev.sh both

# 執行 lint
npm run lint

# 驗證檔案頭規範
python scripts/validate_file_headers.py

# 測試 Git Hook
git commit --allow-empty -m "test: verify hooks"
```

---

## 修改歷史

| 日期 | 版本 | 修改者 | 修改內容 |
|------|------|--------|----------|
| 2026-02-01 | 1.2 | Claude Opus 4.5 | 移除 python-ocr 相關內容（改用雲端 VLM 服務） |
| 2026-02-01 | 1.1 | Claude Opus 4.5 | 新增 CLAUDE.md、Rules、Skills、Hooks 詳細檢測報告章節 |
| 2026-02-01 | 1.0 | Claude Opus 4.5 | 初始版本 |

---

**報告產生時間**: 2026-02-01 21:15 (UTC+8)
**報告產生者**: Claude Opus 4.5
