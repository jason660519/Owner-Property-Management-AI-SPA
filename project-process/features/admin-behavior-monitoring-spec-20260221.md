# 功能規格：超級管理員-網站行為監控與紀錄功能 — 2026/02/21

> 由 HTML 遷移為 Markdown，以利 AI 讀取與版本控制。原始檔：`admin-behavior-monitoring-spec-20260221.html`

---

# 功能規格：超級管理員-網站行為監控與紀錄功能

超級管理員

0%

待開發

2026/02/21

5 SP

中

## 一、功能描述

超級管理員可透過此功能即時監控平台上所有使用者的行為軌跡，包含頁面瀏覽、操作紀錄、異常行為偵測，並生成統計報表供管理決策使用。

## 二、驗收標準 (Acceptance Criteria)

- 系統需記錄所有使用者的頁面訪問紀錄，包含時間戳、IP 位址、使用者 ID、頁面路徑

- 提供每日 / 每週 / 每月流量統計報表，以圖表形式呈現（折線圖、長條圖）

- 異常行為需自動標記並通知管理員（如短時間內大量請求、異地登入）

- 日誌保存期限至少 90 天，超過後自動封存至冷儲存；可手動查詢封存資料

- 需支援依使用者、日期範圍、頁面路徑、動作類型的多條件篩選搜尋

## 三、頁面版面示意

┌─────────────────────────────────────────────────────────┐
│  網站行為監控                          [篩選] [匯出報表] │
├──────────┬──────────┬──────────┬──────────┬────────────┤
│  總瀏覽數 │ 活躍用戶  │  異常事件 │  今日新增 │  平均停留  │
│  12,430  │   248    │    3     │   892    │   4m 32s   │
├─────────────────────────────────────────────────────────┤
│  [折線圖：最近30天流量趨勢]                              │
├─────────────────────────────────────────────────────────┤
│  篩選：用戶 [___]  日期 [____~____]  頁面 [___]  [搜尋] │
├────┬──────────┬──────────────┬────────────┬────────────┤
│時間│用戶       │頁面路徑      │IP          │動作        │
├────┼──────────┼──────────────┼────────────┼────────────┤
│... │  ...     │  /dashboard  │  1.2.3.4   │  PAGE_VIEW │
└────┴──────────┴──────────────┴────────────┴────────────┘

## 四、資料模型

| 欄位 | 類型 | 說明 |
| --- | --- | --- |
| `id` | UUID | 紀錄唯一識別碼 |
| `user_id` | UUID (FK) | 使用者 ID（可為 NULL 對應未登入訪客） |
| `page_path` | VARCHAR(500) | 被訪問的頁面路徑 |
| `action_type` | ENUM | PAGE_VIEW / CLICK / FORM_SUBMIT / API_CALL |
| `ip_address` | INET | 使用者 IP（加密儲存） |
| `user_agent` | TEXT | 瀏覽器與裝置資訊 |
| `created_at` | TIMESTAMPTZ | 事件發生時間 |
| `is_anomaly` | BOOLEAN | 是否被標記為異常行為 |
| `metadata` | JSONB | 額外擴充資訊 |

## 五、技術實作方向

前端追蹤：透過 Next.js middleware 或自訂 Analytics Hook，在每個頁面導航時觸發行為記錄 API。

後端儲存：Supabase `behavior_logs` 資料表 + RLS 僅允許 super_admin 角色讀取。

異常偵測：設定規則引擎（如：同一 IP 1分鐘內超過100次請求 → 標記異常並寫入 `security_alerts` 表）。

## 六、相依功能

- 使用者身份驗證系統（需 user_id）

- 超級管理員-網路安全－隱私審計管理功能（共用日誌資料）

- Supabase RLS 策略
