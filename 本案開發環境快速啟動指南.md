# 本案開發環境快速啟動指南


## 🚀 5 分鐘快速啟動

### 前置要求
- macOS 11+
- Docker Desktop
- Node.js 18+

### 快速步驟

```bash
# 1. 進入專案目錄
cd "/Volumes/KLEVV-4T-1/Real Estate Management Projects/Owner Property Management AI SPA"

# 2. 啟動 Docker Desktop（如未運行）
open -a Docker

# 3. 啟動本地 Supabase
supabase start

# 4. 確認 frontend/.env.local 包含以下內容：
# EXPO_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
# EXPO_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_ACJWlzQHlZjBrEguHvfOxg_3BJgxAaH

# 5. 安裝前端依賴（僅首次需要）
cd frontend && npm install

# 6. 啟動開發伺服器（Web 版）
npx expo start --web

# 7. 在瀏覽器訪問
# http://localhost:8081
```

## 📋 專案結構

```
├── frontend/           # React + Expo 前端
├── supabase/           # 本地資料庫配置
│   └── migrations/     # 資料庫遷移檔
├── docs/               # 專案文件與規劃
└── setup-phase-one.sh  # 自動化啟動腳本
```

## 🌐 本地服務位址

| 服務               | 位址                   |
| ------------------ | ---------------------- |
| API                | http://localhost:54321 |
| Studio（管理後台） | http://localhost:54323 |
| Expo               | http://localhost:8081  |

## 📚 更多資訊

- 詳細設置：[`setup-phase-one.sh`](./setup-phase-one.sh)
- 部署與環境說明：[三階段部署說明](./docs/開發環境+測試環境+上線部署指南/三階段部署說明.md)
- 部署進度與檢查清單：[三階段進度檢測報告](./docs/開發環境+測試環境+上線部署指南/三階段進度檢測報告.md)

## 💻 常用命令

```bash
# Supabase
supabase start      # 啟動服務
supabase stop       # 停止服務
supabase status     # 查看狀態
supabase db reset   # 重置資料庫

# 前端
cd frontend && npm install    # 安裝依賴
npx expo start                # 啟動開發
npx expo start --web          # Web 預覽
npm run android               # Android 預覽
npm run ios                   # iOS 預覽
```

## 🔧 故障排查

若遇到問題，請先依序確認：

1. Supabase 是否已成功啟動（`supabase status`）。
2. `frontend/.env.local` 是否已正確設定 `EXPO_PUBLIC_SUPABASE_ANON_KEY`。
3. Expo 是否已於正確專案目錄啟動（`cd frontend && npx expo start`）。

仍無法排除時，請參考 [三階段進度檢測報告](./docs/開發環境+測試環境+上線部署指南/三階段進度檢測報告.md) 中的故障排查段落。

---

## ✅ 最新測試狀態（2026-01-22）

### 前後端連線測試結果

| 項目              | 狀態   | 備註                           |
| ----------------- | ------ | ------------------------------ |
| 前端應用          | ✅ 正常 | http://localhost:8081 成功載入 |
| Supabase API      | ✅ 正常 | REST API 可正常訪問            |
| Supabase Studio   | ✅ 正常 | 管理介面正常運作               |
| PostgreSQL 資料庫 | ✅ 正常 | 包含 6 個資料表                |
| 前後端連線        | ✅ 正常 | Dashboard 成功渲染數據         |

### 資料庫現有表格
- ✅ `building_title_records` - 建物權狀記錄
- ✅ `land_title_records` - 土地權狀記錄  
- ✅ `property_appointments` - 物件預約
- ✅ `property_photos` - 物件照片
- ✅ `clients` - 客戶資料
- ✅ `owner` - 房東資料

### 已測試功能
- ✅ Docker 容器啟動
- ✅ Supabase 本地服務啟動
- ✅ 前端應用編譯與啟動
- ✅ Web 界面渲染
- ✅ 資料庫連接
- ✅ 環境變數配置

---

**當前版本**：1.1.0（Phase One - Local Development - Tested）

## 版本修訂記錄

- **2026-01-22**：完成前後端連線測試，更新為實際測試通過的啟動步驟與專案路徑，添加測試狀態記錄。
- **2026-01-17**：將文件定位為房東物件管理專案之本地開發快速啟動指南，統一為繁體中文說明，並修正部署相關文件連結與故障排查指引。
