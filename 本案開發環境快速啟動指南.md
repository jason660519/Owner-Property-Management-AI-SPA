# 本案開發環境快速啟動指南


## 🚀 5 分鐘快速啟動

### 前置要求
- macOS 11+
- Docker Desktop
- Node.js 18+

### 快速步驟

```bash
# 1. 進入專案目錄
cd "/Users/jason66/Owner Real Estate Agent SaaS"

# 2. 啟動本地 Supabase（僅需首次）
supabase start

# 3. 記下輸出的 "anon key"

# 4. 更新前端環境變數
echo "EXPO_PUBLIC_SUPABASE_ANON_KEY=<貼上 anon key>" >> frontend/.env.local

# 5. 安裝依賴
cd frontend && npm install

# 6. 啟動開發伺服器
npx expo start

# 7. 在手機上掃描 QR Code
```

## 📋 專案結構

```
├── frontend/           # React + Expo 前端
├── supabase/           # 本地資料庫配置
│   └── migrations/     # 資料庫遷移檔
├── docs/               # 專案文件與規劃
└── setup-phase-one.sh  # 自動化啟動腳本
```

## 🌐 本地服務位址

| 服務                | 位址                      |
|---------------------|---------------------------|
| API                 | http://localhost:54321    |
| Studio（管理後台）  | http://localhost:54323    |
| Expo                | http://localhost:8081     |

## 📚 更多資訊

- 詳細設置：[`setup-phase-one.sh`](./setup-phase-one.sh)
- 部署與環境說明：[三階段部署說明](./docs/開發環境+測試環境+上線部署指南/三階段部署說明.md)
- 部署進度與檢查清單：[三階段進度檢測報告](./docs/開發環境+測試環境+上線部署指南/三階段進度檢測報告.md)

## 💻 常用命令

```bash
# Supabase
supabase start      # 啟動服務
supabase stop       # 停止服務
supabase status     # 查看狀態
supabase db reset   # 重置資料庫

# 前端
cd frontend && npm install    # 安裝依賴
npx expo start                # 啟動開發
npx expo start --web          # Web 預覽
npm run android               # Android 預覽
npm run ios                   # iOS 預覽
```

## 🔧 故障排查

若遇到問題，請先依序確認：

1. Supabase 是否已成功啟動（`supabase status`）。
2. `frontend/.env.local` 是否已正確設定 `EXPO_PUBLIC_SUPABASE_ANON_KEY`。
3. Expo 是否已於正確專案目錄啟動（`cd frontend && npx expo start`）。

仍無法排除時，請參考 [三階段進度檢測報告](./docs/開發環境+測試環境+上線部署指南/三階段進度檢測報告.md) 中的故障排查段落。

---

**當前版本**：1.0.0（Phase One - Local Development）

## 版本修訂記錄

- **2026-01-17**：將文件定位為房東物件管理專案之本地開發快速啟動指南，統一為繁體中文說明，並修正部署相關文件連結與故障排查指引。
